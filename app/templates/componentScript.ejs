'use strict';

const React = require('react');
const { Component, PropTypes } = React;

require('./<%= componentNameCamelCase %>.less');

class <%= componentNamePascalCase %> extends Component {
    render() {
        return (
            <div className="<%= componentNameKebabCase %>">
            </div>
        );
    }
}

<%= componentNamePascalCase %>.propTypes = {

};

<%= componentNamePascalCase %>.defaultProps = {

};

module.exports = <%= componentNamePascalCase %>;

